import React from "react";

function FunctionalComponent() {
    return (
        <p>Here is a component with no state, sort of...except now we get React hooks so it can be.</p>
    )
}

export default FunctionalComponent;